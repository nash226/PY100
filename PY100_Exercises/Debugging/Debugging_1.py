# For both of the function calls we receive a TypeError
# The 1st function call passes too many arguments we need one iterable as our argument. 
# The 2nd function call passes a non iterable argument: a single int
